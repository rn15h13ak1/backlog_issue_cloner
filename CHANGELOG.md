# Changelog

[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 形式に概ね準拠し、
[セマンティックバージョニング](https://semver.org/lang/ja/) に従います。

バージョンはコミット履歴からさかのぼって付けたもので、タグは打っていません。

## [Unreleased]

### Fixed
- PyYAML が入っていない Python で本体（`backlog_issue_cloner.py`）を直接起動すると、
  トレースバックのまま終了コード `1` を返していた。 **`1` は対応表に無い未定義の値。**
  メニューには以前から案内があり、本体だけ抜けていた。案内を出して `2`（実行する前に
  人が直すもの）を返すようにした。指摘は `../proposals/dependency-guard-on-cli-entry.md`

### Added
- `LICENSE`（MIT）。 **公開リポジトリでありながら、利用条件を示すものがどこにも無かった。**
  ライセンスの無い公開リポジトリは既定で全権利が著作権者に留保され、閲覧はできても利用・
  改変・再配布の許諾が無い状態になる。同じ作者の `../docmold` ほか 3 件と揃えて MIT とした。
  README にもライセンスの節を置いている。指摘は `../proposals/public-repos-missing-license.md`
- 依存不足の案内に、実行中のインタプリタのパスを加えた（本体・メニューとも）。
  Windows では `py` ランチャ経由で複数の Python が入っていることがあり、入れた先と
  動かしている先が違うと「`pip install` したのに直らない」から抜け出せないため
- `tests/test_environment.py`。依存が入っていない状態を `sitecustomize` で再現し、
  案内・Exit code・実行中のパスを検査する。 **開発機には依存が入っているため、
  この経路が壊れても他のテストは全部通る。** 細工が空振りしても気付けるよう、
  依存がある場合に通常起動することも併せて確かめている
- `--show-source` のテストを追加した（11 件）。コピー元の種別とカスタム属性を一覧表示する
  機能で、 **これまでテストが 1 件も無かった。** カスタム属性の必須エラーを追うときの
  唯一の手掛かりになる機能であり、表示が実態とずれると調査が誤った方向へ進む。
  実際に 4 通り壊して、いずれも検知することを確認した

## [0.8.0] - 2026-09-21

共通規約に合わせて、ファイルの置き場と名前、コミット前の検査を揃えました。
**`tools/` と `config.sample.yaml` の名前が変わります。** ツールの動作に変更はありません。

### Changed
- 保守用スクリプトの置き場を `tools/` から `scripts/` に変えた。共通規約の記載漏れ検査
  （`check-commands.sh`）が `scripts/` と `bin/` しか見ないため、`tools/` のままでは
  **検査の対象外だった。** 移した直後に `mutation_test.py` の記載漏れが見つかっている。
  あわせてシェバンを付け、実行権を与えた
- 設定のテンプレートを `config.sample.yaml` から `config.example.yaml` に改名した。
  共通規約が `*.example.*` を定型としているため
- `.gitignore` を共通規約の定型に揃えた。`.env` ・ `credentials*` ・ `*.pem` ・ `*.key` などの
  資格情報の型と、`.claude/` ・ エディタの作業ファイルが抜けていた。`config.yaml` の除外は
  従来どおり
- コミット前の検査に `check-commands.sh` を追加した。共通規約が「検査が 3 つを超えたら
  手順そのものも検査する」と定めており、本リポジトリは 5 つで該当する。手順に書き忘れた
  検査は実行されず、実行されない検査は無いのと同じになるため
- `CLAUDE.md` に、`check-terms.sh` と `gen-decision-index.py` を使わない理由を明記した。
  どちらも ADR 向けで、本リポジトリは ADR を置いていない。`check-terms.sh` は規則ファイルが
  無いと素通りするため、手順に並べると実行したつもりになる

## [0.7.1] - 2026-09-20

リンク切れを見逃さないよう検査を強化し、本リポジトリ固有の取り決めを記録しました。
ツールの動作に変更はありません。

### Added
- `tools/check_docs.py` のリンク検査で、他ファイルの見出しアンカーも見るようにした。
  これまでは同じファイル内のアンカーしか確認しておらず、`../README.md#開発` のような
  飛び先の無いリンクを見逃していた。飛び先が無くてもリンクは押せてしまい、ファイルの
  先頭に着くだけなので気づけない。故意に 5 通り壊して、すべて検知することを確認した
- `CLAUDE.md` に、共通規約から逸脱して commit / push を自動で行うことと、その理由を
  記録した。コミット前に実行する 4 つの検査も明記している

### Fixed
- `docs/DESIGN.md` から README への `#開発` へのリンクが、存在しない見出しを
  指していた。`#テスト` に修正した

### Changed
- `docs/DESIGN.md` の「md を書くときの体裁」に、リンクの飛び先の実在を検査する項を
  加えた。README にも他ファイルの見出しアンカーを見る旨を補った

### Removed
- `CLAUDE.md` から、`check-privacy.sh` の指摘 2 件を誤検知として許容する記述を削除した。
  プレースホルダを資格情報と誤認する問題が ws-conventions 側で解消され、指摘が 0 件に
  なったため。代わりに、4 つの検査がすべて 0 件でなければコミットしない旨を明記した

## [0.7.0] - 2026-09-16

ドキュメントを読み手ごとに分け、体裁の検査を自動化しました。

### Added
- `docs/EXAMPLES.md`。3 つのモードそれぞれについて、メニューの操作から結果までを
  通しで載せた実行例。README からは目次・本文・ファイル構成の 3 箇所でリンクする
- `tools/check_docs.py` に md の体裁の検査を追加。`**` による強調の両端が半角
  スペースか、` ``` ` の直後の罫線行との間に空行があるかを見る。強調の中に
  インラインコードを挟んだ書き方も検出できるよう、コードを退避してから判定している

### Changed
- README を社内の他ツール（docmold）の構成に揃えた。冒頭に 1 行の定義と主要機能、
  グループ分けした目次、クイックスタートを置き、使い方を番号付きのガイドに整理した。
  「終了コード」は「Exit code」に、リファレンスは表中心に統一
- コマンド例を Windows と macOS / Linux の併記にした。全体の読み替えは動作要件の
  注記で示している
- 設計判断の根拠は `docs/DESIGN.md` に集約済みのため、README からは重複する背景
  説明を外し、設計方針の節からリンクする形にした（824 行 → 654 行）
- md の `**` による強調の両端に半角スペースを入れた（67 行）。日本語文字に直接
  隣接すると環境によって強調が描画されないため。句読点や括弧に隣接する箇所も
  例外なく入れている
- ` ``` ` の直後が罫線だけの行になっていた箇所に空行を入れた。プレビュー表示が
  崩れるため

## [0.6.0] - 2026-09-10

Windows でメニューをダブルクリック起動できるようにしました。

### Added
- `menu.bat`。ASCII のみ・CRLF で記述し、`cd` ではなく `pushd "%~dp0"` を使う
  （`cd` はネットワーク共有の UNC パスで失敗するため）。`py` ランチャを優先し、
  無ければ `python` にフォールバックする。Python が見つからない場合と終了コードが
  0 以外の場合を表示して `pause` する
- `.gitattributes` で `*.bat` を `eol=crlf` に固定。Git が LF に正規化すると
  cmd.exe が誤動作するため
- `tests/test_windows.py`。出力ソースの全文字が CP932 で表現できるか、`menu.bat` が
  CRLF・ASCII・`pushd`・`pause` を満たすかを検査する。macOS では気づけない退行のため

### Fixed
- em dash（U+2014）が CP932 に無く、Windows の日本語コンソールでは文字化けではなく
  `UnicodeEncodeError` で落ちていた。メニューのモード一覧が「単純複製 ― 説明」の
  形式だったため、最初の画面すら表示できない状態だった。CP932 にある horizontal
  bar（U+2015）へ 35 箇所を置換した。コンソールの設定に依存しないよう、`chcp` では
  なく文字側で対処している
- PyYAML が入っていない環境で、トレースバックではなくインストール方法を表示して
  終了するようにした

## [0.5.0] - 2026-09-09

複製もれを解消し、保守用の仕組みを整えました。

### Added
- カスタム属性の引き継ぎ。コピー元の値を作成 API のパラメータ（`customField_<id>`）へ
  変換して渡す。種別ごとに変換を分けており、単一リスト・ラジオは選択肢の ID、
  複数リスト・チェックボックスは ID の配列、日付は ISO 形式から日付部分を切り出す。
  「その他」の入力値も併せて渡す。`clone.copy_custom_fields` で無効にできる
- `--show-source`。コピー元の種別とカスタム属性を ID・種別・現在の値つきで一覧表示し、
  作成時に渡す値と、値が未設定の属性を示して終了する。作成・更新は行わない
- `clone.custom_fields`。コピー元に値が無い必須属性を「カスタム属性の ID: 値」で
  補える。コピー元から引き継いだ値より優先し、親課題と子課題の双方に適用する
- 担当者・カテゴリー・マイルストーン・発生バージョン・予定時間の引き継ぎ
  （`clone.copy_attributes` で無効にできる）
- `docs/DESIGN.md`。保守する人向けに、前提・全体構成・処理の流れ・Backlog API への
  依存・設計判断と根拠・落とし穴・変更ガイド・テスト方針・未解決の事項を記載
- `tools/check_docs.py`。設定項目・コマンドラインオプション・終了コードが、実装と
  README と `config.sample.yaml` で一致しているかを検査する。リンク切れも検出する
- `tools/mutation_test.py`。実装にわざとバグを埋め込み、テストが落ちるかを測る。
  過去に実際に起きた不具合を再現する変異には `【再現】` を付けている

### Changed
- 親課題の種別をコピー元から引き継ぐようにした。解決の優先順位は
  「設定の `issue_type` → コピー元と同じ種別 → プロジェクトの最初の種別」
- 親課題の優先度も同様にコピー元から引き継ぐようにした
- 子課題を件名順に複製するようにした。API の返却順は一定でないため、並べないと
  実行のたびに採番される課題キーの順が変わる。数字は数値として比較するので
  `手順1 → 手順2 → 手順10` と並ぶ。件名が同じ場合は課題 ID で順序を確定させる
- 更新で「変更なし」と判断した際に、サーバのメッセージを併記するようにした。
  Backlog は必須項目エラーにも同じ `code=7` を返すため、取り違えたまま正常終了する
  事故を避けるため
- テストを `tests/` ディレクトリに移した

### Fixed
- 親課題の種別が引き継がれず、プロジェクトの最初の種別で作成されていた。Backlog は
  カスタム属性の必須設定を種別ごとに持つため、コピー元では不要な属性が必須の種別で
  作成され、`作業完了チェックは必須です（code=7）` で失敗していた。子課題は元々
  引き継いでおり、親だけ挙動が違っていた
- 親課題の優先度が引き継がれず、常に「中」で作成されていた。種別と同じ構造の欠陥で、
  エラーにならないぶん気づきにくい状態だった
- 課題の作成時にカスタム属性を渡しておらず、必須のカスタム属性があるプロジェクトでは
  作成が 400 で失敗していた

## [0.4.0] - 2026-09-09

対話メニューを追加し、プロジェクトを跨いだ複製を禁止しました。

### Added
- 対話メニュー（`menu.py`）。3 つのモードを選び、課題キーをその場で入力して実行する。
  モードごとに必要な項目だけを尋ね、件名テンプレートに `{YYYYMMDD}` が無ければ日付を
  尋ねない。前回の入力を記憶して次回の既定値にする。本体は subprocess で呼び、
  実行内容の一覧と最終確認は本体に任せる
- 設定を上書きする CLI オプション（`--source-issue-key` / `--target-issue-key` /
  `--summary-template` / `--no-summary-template`）。設定ファイルには接続情報だけを
  置き、複製の対象を実行時に指定できる

### Changed
- プロジェクトを跨いだ複製を廃止した。更新系のツールを複数人で使うため、
  プロジェクトを取り違えて課題を作る事故を防ぐ方針とした。`clone.target_project_key`
  は廃止し、設定に残っている場合は黙って無視せずエラーで停止する。無視すると意図した
  別プロジェクトではなくコピー元のプロジェクトに作られてしまうため
- `target_issue_key` に別プロジェクトの課題を指定した場合も、API を呼ぶ前に停止する
- `source_issue_key` のサンプル値（`PROJ-123`）判定を廃止した。実在しうるキーであり、
  メニューから正当な入力として渡されると弾かれてしまうため

### Fixed
- メニューで `10_AAA-20` のような課題キーを入力すると、形式が不正と判定されて
  聞き返されていた。Backlog のプロジェクトキーは数字やアンダースコアで始められる

## [0.3.0] - 2026-09-09

子課題の複製と 3 つの動作モードに対応しました。

### Added
- 親課題を指定すると子課題もまとめて複製する。複製先の親に紐づく既存の子課題と
  件名で突き合わせ、子ごとに新規作成 / 本文更新 / 変更なしを決める。照合は複製先の
  親に紐づく子課題の中だけで行うため、プロジェクト全体のキーワード検索は使わない。
  完了済みの子課題も照合対象に含める（再作成を避けるため）
- 子課題の種別はコピー元と同名のものを複製先で探して使い、無ければ親と同じ種別に
  フォールバックする。優先度は Backlog スペース共通のため ID をそのまま使う
- `clone.include_children`（既定 `true`）と `clone.child_summary_template`
  （既定 `{SOURCE_SUMMARY}`）
- `clone.target_issue_key`。件名による検索を行わず、その課題（と子課題）を直接更新
  する。件名は変更せず、更新するのは本文のみ
- 複製先で件名が変わった子課題の警告。照合できなかった子課題があり、かつ新規作成が
  発生する場合に、確認プロンプトの前に課題キーと件名を表示する。作成が無ければ重複は
  起きないので黙っている

### Changed
- 確認プロンプトを 1 回にまとめた。課題ごとに尋ねると子課題の数だけ確認が発生する
  ため、実行内容を一覧表示してまとめて確認する方式にした
- `clone.summary_template` を省略可能にした。省略時はコピー元の件名をそのまま使う
- `summary_template` と `target_issue_key` のどちらも無い場合は、複製先を特定する
  手掛かりがないため重複チェックを行わず、実行のたびに新しい課題を作る。コピー元と
  同じ件名で重複チェックしたい場合は `{SOURCE_SUMMARY}` を明記する
- 終了コードは親子をまとめた結果を返す（作成があれば 10、無く更新があれば 11、
  すべて変更なしなら 0）

### Fixed
- 件名テンプレートを省略すると、生成件名がコピー元と同一になるため重複チェックで
  コピー元自身がヒットし、複製が作られないまま変更なしで終わっていた

## [0.2.0] - 2026-08-29

無人実行に対応し、ドキュメントとテストを整備しました。

### Added
- `--yes`。確認プロンプトを出さずに実行する。非対話環境では確認を求めず、`--yes`
  なしなら `input` を呼ばずにスキップして終了コード 20 を返す
- `--detailed-exit-code`。正常終了時の結果を区別する（0 変更なし / 10 作成 /
  11 更新）。既定は従来どおり 0 のままとし、`cmd || alert` 形式の誤検知を避ける
- 一時的な失敗のリトライ。429 / 5xx / 接続エラーを指数バックオフで再試行する。
  429 の `Retry-After` ヘッダを優先し、`retry_max_delay` で上限を設ける
- `clone.match_mode` に `exact` を追加（既定は従来どおり `substring`）
- `clone.include_closed`。既定で完了済み課題を検索対象から除外し、過去にクローズ
  した同名課題による新規作成のスキップを防ぐ
- README とユニットテスト

### Changed
- `sys.exit` の直呼びを廃止し、`ConfigError` / `BacklogError` を送出して `main()` で
  終了コードに変換する構成にした
- 更新系リクエスト（POST / PATCH）の再試行を、リクエストがサーバに届いていないことが
  確実な失敗のみに限定した。届いた後に失敗した可能性があるケースで再送すると、
  サーバ側では成功していたときに課題が二重に作られるため。429 と接続失敗のみ再試行し、
  5xx・タイムアウト・接続断では再試行しない
- 種別・優先度の解決を新規作成の経路まで遅延させ、更新・変更なしの経路で API 呼び出しを
  2 回削減した（本文更新 5 回 → 3 回、変更なし 4 回 → 2 回）
- 重複チェックの課題検索を遅延列挙にし、最初にマッチした時点で以降のページを取得
  しないようにした
- コピー元と同じプロジェクトの場合は `source_issue["projectId"]` を流用し、
  `get_project` の呼び出しを省略するようにした

### Fixed
- cron から実行すると `EOFError` を無言で握りつぶし、1 件も処理せず終了コード 0 を
  返していた
- レスポンス待ちや読み込み中のタイムアウトが `URLError` ではなく `TimeoutError` で
  送出されるため捕捉できておらず、トレースバックが出て終了コード 1 になっていた
- 空の `config.yaml` や中身が空のセクションで `AttributeError` のトレースバックが
  出ていた。`ssl_verify: "false"` のような文字列が真と評価される誤設定も弾くようにした

## [0.1.0] - 2026-04-09

最初の実装。

### Added
- 指定した課題の本文をコピーして新規課題を作成する CLI ツール
- 件名テンプレートの `{YYYYMMDD}` を `--date` または今日の日付に展開する
- 同じ件名の課題を検索し、無ければ作成・本文に差分があれば更新する
- 既定はドライラン。`--execute` を付けたときだけ Backlog を書き換える
- 設定ファイル（`config.yaml`）による接続情報とクローン設定の指定
- オンプレ版向けの `ssl_verify` と `base_path`
